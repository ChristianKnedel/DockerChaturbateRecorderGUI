#docker-compose.yml
version: '2'

services:    
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: recorder_app
    mem_limit: '256M'
    restart: always
    environment:
      TZ: "Europe/Berlin"
      HOST_MEDIA: "/Users/CharlieScene/NetBeansProjects/chatrubate/src/interface/media/videos/"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./interface/media:/code/media
    ports:
      - "8002:8000"
      
     
